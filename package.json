{
  "name": "skibum2025",
  "private": true,
  "scripts": {
    "build:catalog": "node scripts/build-catalog.js",
    "check:catalog": "node scripts/build-catalog.js --check",
    "test": "node --test tests/*.test.js",
    "check": "npm run check:catalog && npm test",
    "fetch:snow": "node scripts/fetch-snow-data.js",
    "test:browser": "playwright test",
    "build:config": "node scripts/build-config.js",
    "build": "npm run build:catalog && npm run build:config"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@babel/standalone": "7.29.9",
    "@playwright/test": "1.63.0",
    "jsdom": "26.1.0",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}
